[build]
builder = "railpack"
buildCommand = "npm run build && cd comrade && python manage.py collectstatic --noinput"

[deploy]
startCommand = "cd comrade && python manage.py migrate --noinput && daphne -b 0.0.0.0 -p $PORT comrade.asgi:application"
healthcheckPath = "/api/auth/google-config/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
